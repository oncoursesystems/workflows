name: web-cd

on:
  workflow_call:
    inputs:
      runs-on:
        type: string
        default: k8s-linux

jobs:
  docker-publish:
    uses: ./.github/workflows/docker-build.yml
    with:
      runs-on: ${{ inputs.runs-on }}
